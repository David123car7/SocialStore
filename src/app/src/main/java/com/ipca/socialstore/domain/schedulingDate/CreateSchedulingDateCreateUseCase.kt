package com.ipca.socialstore.domain.schedulingDate

import com.ipca.socialstore.data.repository.SchedulingDateRepository
import com.ipca.socialstore.data.resultwrappers.ResultWrapper
import javax.inject.Inject
/*
class CreateSchedulingDateCreateUseCase @Inject constructor(private val schedulingDateRepository: SchedulingDateRepository) {
    suspend operator fun invoke(schedulingDate : SchedulingDateModel): ResultWrapper<Int>{
        return schedulingDateRepository.createSchedulingDate(schedulingDate = schedulingDate)
    }
}

 */